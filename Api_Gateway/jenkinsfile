node {
  stage('SCM') {
    checkout scm
  }
  stage('SonarQube Analysis') {
    steps {
                withSonarQubeEnv('sonarqube') {
                    sh "${mavenPath} -Psonar sonar:sonar"
                }
  }
}
}
