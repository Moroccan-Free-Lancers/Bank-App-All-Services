node {
  stage('SCM') {
    checkout scm
  }
  stage('SonarQube Analysis') {
                withSonarQubeEnv('sonarqube') {
                    sh "${mavenPath} -Psonar sonar:sonar"
                }
  
}
}
