node {
  stage('SCM') {
    checkout scm
  }
  stage('SonarQube Analysis') {
                withSonarQubeEnv('sonarqube') {
                    sh "mvn clean verify sonar:sonar -Dsonar.projectKey=Backend-test -Dsonar.login=admin -Dsonar.password=lamya2210"
                }
  
}
}
